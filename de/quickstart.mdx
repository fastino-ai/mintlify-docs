---
title: "Fastino-Schnellstart: erste Inferenz oder erstes Training"
description: "Erstellen Sie einen Fastino-API-Schlüssel, listen Sie Basismodelle auf, führen Sie die erste Inferenz mit GLiNER aus und starten Sie optional ein Training."
sidebarTitle: "Schnellstart"
---

Dieser Leitfaden führt Sie auf dem schnellsten Weg zu einer funktionierenden Fastino-Integration. Am Ende haben Sie einen erfolgreichen Inferenzaufruf getätigt und verstehen den Aufbau der API. Alles, was Sie benötigen, ist ein Fastino-Konto und ein Terminal.

<Steps>
  <Step title="Registrieren und Ihren API-Schlüssel abrufen">
    Erstellen Sie ein Konto unter [Fastino](https://agent.fastino.ai/).

    Sobald Sie angemeldet sind, gehen Sie zu **Settings → API Keys** und generieren Sie einen neuen Schlüssel. Kopieren Sie ihn an einen sicheren Ort, da Sie ihn nach dem Schließen des Dialogs nicht mehr einsehen können.

    <Warning>
      Halten Sie Ihren API-Schlüssel aus der Versionskontrolle heraus. Verwenden Sie eine Umgebungsvariable oder einen Secrets-Manager, anstatt ihn in Ihren Quelldateien fest zu codieren.
    </Warning>

    Setzen Sie Ihren Schlüssel als Umgebungsvariable, damit die folgenden Beispiele unverändert funktionieren:

    ```bash
    export FASTINO_API_KEY="your_api_key_here"
    ```
  </Step>
  <Step title="Verfügbare Basismodelle auflisten (optional)">
    Sie können die in Fastino verfügbaren Modelle durchsuchen. Verwenden Sie `GET /v1/base-models`, um den vollständigen Katalog zu sehen.

    <CodeGroup>

    ```bash curl
    curl https://api.fastino.ai/v1/base-models \
      -H "X-API-Key: $FASTINO_API_KEY"
    ```

    ```python Python
    import os
    import requests
    
    response = requests.get(
        "https://api.fastino.ai/v1/base-models",
        headers={"X-API-Key": os.environ["FASTINO_API_KEY"]}
    )
    print(response.json())
    ```

    ```javascript JavaScript
    const response = await fetch(
      "https://api.fastino.ai/v1/base-models",
      {
        headers: {
          "X-API-Key": process.env.FASTINO_API_KEY
        }
      }
    );
    const data = await response.json();
    console.log(data);
    ```

    </CodeGroup>

    Die Antwort listet Modell-IDs auf, die Sie an `/v1/chat/completions` übergeben können. Zum Beispiel ist `fastino/gliner2-base-v1` das GLiNER-Basismodell für NER-Aufgaben.
  </Step>
  <Step title="Ihren ersten Inferenzaufruf ausführen">
    Rufen Sie `POST /v1/chat/completions` mit einem Modell, einer Nachricht und einem Schema auf, das definiert, was Sie extrahieren möchten. Das folgende Beispiel verwendet das GLiNER-Basismodell, um benannte Entitäten aus einem Satz zu extrahieren.

    <CodeGroup>

    ```bash curl
    curl -X POST "https://api.fastino.ai/v1/chat/completions" \
      -H "Authorization: Bearer $FASTINO_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "model": "fastino/gliner2.5-multi-v1",
        "messages": [
          {
            "role": "user",
            "content": "Apple announced the MacBook Pro at WWDC in Cupertino."
          }
        ],
        "schema": {
          "entities": [
            {
              "name": "organization",
              "description": "business or institution name"
            },
            {
              "name": "product",
              "description": "named commercial product"
            },
            {
              "name": "event",
              "description": "named conference or event"
            },
            {
              "name": "location",
              "description": "city, region, or place"
            }
          ]
        },
        "threshold": 0.5
      }'
    ```

    ```python Python
    import json
    import os
    
    import requests
    
    response = requests.post(
        "https://api.fastino.ai/v1/chat/completions",
        headers={
            "Authorization": f"Bearer {os.environ['FASTINO_API_KEY']}",
            "Content-Type": "application/json",
        },
        json={
            "model": "fastino/gliner2.5-multi-v1",
            "messages": [
                {
                    "role": "user",
                    "content": "Apple announced the MacBook Pro at WWDC in Cupertino.",
                }
            ],
            "schema": {
                "entities": [
                    {"name": "organization", "description": "business or institution name"},
                    {"name": "product", "description": "named commercial product"},
                    {"name": "event", "description": "named conference or event"},
                    {"name": "location", "description": "city, region, or place"},
                ]
            },
            "threshold": 0.5,
        },
    )
    
    response.raise_for_status()
    result = response.json()
    entities = json.loads(result["choices"][0]["message"]["content"])
    
    print(json.dumps(entities, indent=2))
    ```

    ```javascript JavaScript
    const response = await fetch(
      "https://api.fastino.ai/v1/chat/completions",
      {
        method: "POST",
        headers: {
          Authorization: `Bearer ${process.env.FASTINO_API_KEY}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          model: "fastino/gliner2.5-multi-v1",
          messages: [
            {
              role: "user",
              content: "Apple announced the MacBook Pro at WWDC in Cupertino.",
            },
          ],
          schema: {
            entities: [
              { name: "organization", description: "business or institution name" },
              { name: "product", description: "named commercial product" },
              { name: "event", description: "named conference or event" },
              { name: "location", description: "city, region, or place" },
            ],
          },
          threshold: 0.5,
        }),
      }
    );
    
    if (!response.ok) {
      throw new Error(`Inference failed: ${response.status} ${await response.text()}`);
    }
    
    const result = await response.json();
    const entities = JSON.parse(result.choices[0].message.content);
    
    console.log(JSON.stringify(entities, null, 2));
    ```

    </CodeGroup>

    Bei Encoder-Modellen (GLiNER) definiert das Objekt `schema` die Aufgabe und kann `entities`, `classifications`, `structures` und `relations` enthalten:

    - `entities` — eine Liste von Entitätstyp-Strings für NER
    - `classifications` — eine Liste von `{task, labels}`-Objekten für Textklassifikation
    - `structures` — ein Dictionary mit Strukturdefinitionen für die JSON-Extraktion
    - `relations` — eine Liste von Relationsdefinitionen

    Bei Decoder-Modellen (LLMs) lassen Sie `schema` weg und senden eine normale Chat-Nachricht.

    <Note>
      `model` muss eine inferenzfähige Basismodell-ID oder die UUID eines abgeschlossenen, deploybaren Trainingsjobs sein. Deployen Sie nach dem Training das ausgewählte Artefakt, bevor Sie die Job-UUID für Inferenz verwenden.
    </Note>
  </Step>
  <Step title="Einen Trainingsjob starten (optional)">
    Wenn Sie bereit sind, mit Ihren eigenen Daten feinabzustimmen, starten Sie einen Trainingsjob.

    <CodeGroup>

    ```bash curl
    curl -X POST https://api.fastino.ai/v1/training-jobs \
      -H "X-API-Key: $FASTINO_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "model_name": "my-ner-model",
        "base_model": "fastino/gliner2-base-v1",
        "datasets": [{"name": "my-dataset"}],
        "training_type": "lora",
        "nr_epochs": 5,
        "learning_rate": 5e-5
      }'
    ```

    ```python Python
    import os
    import requests
    
    response = requests.post(
        "https://api.fastino.ai/v1/training-jobs",
        headers={
            "X-API-Key": os.environ["FASTINO_API_KEY"],
            "Content-Type": "application/json"
        },
        json={
            "model_name": "my-ner-model",
            "base_model": "fastino/gliner2-base-v1",
            "datasets": [{"name": "my-dataset"}],
            "training_type": "lora",
            "nr_epochs": 5,
            "learning_rate": 5e-5
        }
    )
    print(response.json())
    # {"id": "uuid-of-training-job", "status": "requested"}
    ```

    </CodeGroup>

    Die Antwort enthält eine Job-`id`. Fragen Sie `GET /v1/training-jobs/{id}` ab, um den Status zu prüfen. Sobald der Job den Status `complete` erreicht, verwenden Sie die Job-ID als `model` in `/v1/chat/completions`-Aufrufen.

    Job-Statuswerte: `requested` → `running` → `complete` (oder `failed` / `stopped`).
  </Step>
</Steps>

## Nächste Schritte

- [Training-API](/de/training): Erfahren Sie, wie Sie mit unserer Training-API ein GLiNER-Modell End-to-End feinabstimmen.
- [Inferenz-API](/de/inference): Erfahren Sie, wie Sie über unsere API sowohl Basismodelle als auch feinabgestimmte Modelle deployen und Inferenz damit ausführen.
- [Agent-Skill](/de/concepts/gliner-agent-skill): Nutzen Sie diesen Skill, um Inferenz auszuführen oder Trainingsjobs zu starten – alles direkt aus Ihrem Coding-Agent.
- [Beispielanwendungsfälle](/example-use-cases): Suchen Sie nach Ideen, was Sie bauen könnten? Entdecken Sie Beispielanwendungsfälle.
