---
title: "Inférence sur Fastino : API OpenAI et Anthropic"
description: "Exécutez des inférences sur Fastino via les chat completions compatibles OpenAI ou les messages compatibles Anthropic."
---

Une fois que vous disposez d'un modèle entraîné, ou si vous souhaitez utiliser directement un modèle de base, envoyez son ID de modèle ou l'UUID d'un job d'entraînement terminé dans le champ `model`. Fastino achemine automatiquement la requête vers le bon déploiement.

Fastino propose des interfaces compatibles avec OpenAI et Anthropic. Pour l'extraction et la classification GLiNER, utilisez `POST /v1/chat/completions` avec `model`, `messages` et l'extension `schema` de premier niveau de Fastino. Consultez l'[API d'inférence](/fr/inference) pour les formats de requête et de réponse de référence.

<Note>
  Les endpoints de type chat (`/v1/chat/completions`, `/v1/responses`, `/v1/messages`) rejettent les requêtes vers un modèle de base décodeur pré-entraîné (non-instruct) avec un `400`. Utilisez la variante `-Instruct` du modèle.
</Note>

## Format compatible OpenAI

Fastino expose un endpoint compatible OpenAI à `https://api.fastino.ai/v1`. Pointez n'importe quel SDK ou intégration OpenAI existant vers cette URL de base et utilisez votre clé API Fastino. Aucune autre modification n'est nécessaire.

```bash
curl -X POST https://api.fastino.ai/v1/chat/completions \
  -H "Authorization: Bearer $FASTINO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "messages": [
      {"role": "user", "content": "Extract entities from: Apple launched the iPhone."}
    ],
    "schema": {
      "entities": [
        {"name": "organization", "description": "business or institution name"},
        {"name": "product", "description": "named commercial product"}
      ]
    }
  }'
```

Endpoints compatibles OpenAI disponibles :

| Méthode | Endpoint | Description |
| --- | --- | --- |
| `POST` | `/v1/chat/completions` | Chat completions |
| `POST` | `/v1/responses` | API Responses |
| `POST` | `/v1/embeddings` | Créer des embeddings |
| `GET` | `/v1/models` | Lister les modèles disponibles (appelable sans authentification pour le catalogue public) |
| `GET` | `/v1/models/:model_id` | Récupérer les métadonnées d'un modèle unique |

`/v1/models` et `/v1/models/:model_id` sont une infrastructure partagée. Ces deux mêmes routes servent également les appels `models.retrieve(...)` du SDK compatible Anthropic.

<Warning>
  L'ancien endpoint `POST /v1/completions` a été supprimé. Migrez vers `POST /v1/chat/completions` et envoyez le prompt dans un message utilisateur.
</Warning>

<Tip>
  Lorsque vous utilisez le SDK OpenAI Python ou Node, passez les champs spécifiques à Fastino comme `schema` via le paramètre `extra_body`. Par exemple :

  ```python
  client.chat.completions.create(
      model="3fa85f64-5717-4562-b3fc-2c963f66afa6",
      messages=[{"role": "user", "content": "Extract entities from: Apple launched the iPhone."}],
      extra_body={"schema": {"entities": ["organization", "product"]}}
  )
  ```
</Tip>

## Format compatible Anthropic

Fastino expose également un endpoint compatible Anthropic. Définissez le `base_url` de votre SDK sur `https://api.fastino.ai` et utilisez votre clé API Fastino à la place d'une clé Anthropic. Le SDK ajoute lui-même `/v1/messages` et envoie la clé via `x-api-key`.

```bash
curl -X POST https://api.fastino.ai/v1/messages \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "max_tokens": 1024,
    "messages": [
      {"role": "user", "content": "Extract entities from: Apple launched the iPhone."}
    ],
    "schema": {"entities": ["organization", "product"]}
  }'
```

Les endpoints compatibles OpenAI et Anthropic prennent tous deux en charge le streaming (`stream: true`).

## Mise en cache des prompts

La mise en cache des prompts réduit le coût et la latence sur les préfixes de prompt répétés, mais la façon de l'activer dépend de la famille de modèles :

- **Famille OpenAI / GPT** — la mise en cache est **automatique**. Vous n'avez rien à faire ; tout `cache_control` que vous envoyez est ignoré silencieusement plutôt qu'appliqué, il n'y a donc pas besoin de le supprimer si vous faites basculer un client depuis Claude.
- **Style Claude / Anthropic** — la mise en cache est **opt-in par défaut**. Fastino transmet votre requête telle quelle et n'ajoute pas de marqueurs de cache pour vous. Ainsi, à moins que vous n'ajoutiez un marqueur `cache_control` sur la partie stable de votre prompt, le préfixe n'est pas mis en cache et vous payez le tarif d'entrée plein à chaque tour.

Pour mettre en cache le préfixe stable sur un modèle Claude, envoyez le contenu sous forme de tableau de blocs et marquez-le. Cela fonctionne aussi sur `/v1/chat/completions` et `/v1/responses`, pas seulement sur l'endpoint compatible Anthropic :

```bash
curl -X POST https://api.fastino.ai/v1/chat/completions \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-opus-5",
    "messages": [
      {
        "role": "system",
        "content": [
          {
            "type": "text",
            "text": "Large stable system prompt or reusable context goes here.",
            "cache_control": { "type": "ephemeral" }
          }
        ]
      },
      { "role": "user", "content": "What is prompt caching?" }
    ]
  }'
```

Les tokens mis en cache sont facturés à un tarif réduit et sont visibles dans **Paramètres → Crédits**.

Consultez [Mise en cache des prompts](/api-reference/prompt-caching) pour savoir où placer les marqueurs, connaître les tailles minimales, les tarifs, comment lire l'usage des tokens, et obtenir des conseils pour maximiser les hits de cache.

## Désactiver la persistance de l'inférence

Par défaut, Fastino stocke chaque inférence (entrée, sortie et métadonnées) afin d'alimenter l'évaluation, le clustering de cas d'usage et l'entraînement d'adaptateurs. Passez `store: false` pour ignorer la persistance sur une requête spécifique.

```bash
curl -X POST https://api.fastino.ai/v1/chat/completions \
  -H "Authorization: Bearer pio_sk_..." \
  -H "Content-Type: application/json" \
  -d '{
    "model": "claude-opus-5",
    "messages": [
      {"role": "user", "content": "Hello, world!"}
    ],
    "store": false
  }'
```

`store: false` est pris en charge sur `/v1/chat/completions` et `/v1/messages`, avec un comportement identique pour les requêtes en streaming et non-streaming.

### Ce qui change avec `store: false`

|  | Défaut (`store: true`) | `store: false` |
| --- | --- | --- |
| L'inférence s'exécute | Oui | Oui |
| Entrée/sortie stockées | Oui | Non |
| Évaluation exécutée | Oui | Non |
| Clustering de cas d'usage | Oui | Non |
| Alimentation de l'entraînement d'adaptateurs | Oui | Non |
| Facturation des tokens | Oui | Oui |
| `inference_id` dans la réponse | Oui | Oui (pour la corrélation) |

<Note>
  La facturation s'applique toujours. L'usage des tokens, les COGS et la facturation mesurée sont enregistrés même lorsque `store: false` est défini. Seule la charge utile complète requête/réponse n'est pas conservée.
</Note>

### Quand l'utiliser

- **Health checks** — sondes de liveness et de readiness qui s'exécutent en continu - **Benchmarks internes** — évaluations que vous exécutez contre votre propre vérité terrain et qui ne devraient pas polluer l'historique d'inférence côté utilisateur - **Développement et tests** — appels exploratoires pendant le travail d'intégration où l'accumulation de lignes d'inférence ajoute du bruit

## Historique des inférences

Fastino enregistre chaque appel d'inférence. Vous pouvez récupérer les résultats passés et soumettre des corrections pour améliorer les données d'entraînement futures.

```bash
# List recent inferences
curl https://api.fastino.ai/v1/inferences \
  -H "X-API-Key: YOUR_API_KEY"

# Get a specific inference result
curl https://api.fastino.ai/v1/inferences/INFERENCE_ID \
  -H "X-API-Key: YOUR_API_KEY"

# Mark as correct
curl -X POST https://api.fastino.ai/v1/inferences/INFERENCE_ID/feedback \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"verdict": "correct"}'

# Submit a correction
curl -X POST https://api.fastino.ai/v1/inferences/INFERENCE_ID/feedback \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"verdict": "incorrect", "corrected_output": {...}, "notes": "optional reviewer note"}'

# Read back the feedback you (or a teammate) already submitted
curl https://api.fastino.ai/v1/inferences/INFERENCE_ID/feedback \
  -H "X-API-Key: YOUR_API_KEY"
```

`GET .../feedback` renvoie 404 si aucun feedback n'a encore été soumis pour cette inférence. Le champ `notes` sur `POST .../feedback` est optionnel.

Filtres de requête optionnels pour `GET /v1/inferences` : `limit`, `offset`, `model_id`, `task`, `project_id`, `training_job_id`, `latency_min`, `latency_max` (ms), `since`, `until` (bornes ISO 8601 sur `created_at`).

`GET /v1/inferences/INFERENCE_ID` fait également apparaître tout feedback humain déjà soumis (`human_verdict`, `human_corrected_output`, `human_feedback_notes`) directement sur l'enregistrement.
