---
title: "Lister les jobs d'entraînement"
sidebarTitle: "Lister les jobs"
description: "Listez et filtrez les jobs d'entraînement Fastino."
api: "GET /felix/training-jobs"
noindex: true
---

Renvoie les jobs d'entraînement visibles pour le compte authentifié.

## Requête

<ParamField header="X-API-Key" type="string" required>
  Votre clé API Fastino.
</ParamField>

<ParamField query="status" type="string">
  Filtrer par statut de job persisté.
</ParamField>

<ParamField query="project_id" type="string">
  Filtrer par ID de projet.
</ParamField>

<ParamField query="limit" type="integer" default="200">
  Taille de page, de `1` à `200`.
</ParamField>

<ParamField query="offset" type="integer" default="0">
  Nombre de jobs correspondants à ignorer.
</ParamField>

<RequestExample>

```bash cURL
curl "https://api.pioneer.ai/felix/training-jobs?status=complete&limit=50&offset=0" \
  -H "X-API-Key: $PIONEER_API_KEY"
```

</RequestExample>

## Réponse

<ResponseField name="success" type="boolean">
  Indique si la requête a abouti.
</ResponseField>

<ResponseField name="training_jobs" type="object[]">
  Enregistrements des jobs d'entraînement pour cette page.
</ResponseField>

<ResponseField name="count" type="integer">
  Nombre de jobs dans cette page.
</ResponseField>

<ResponseField name="total" type="integer">
  Nombre total de jobs correspondants avant pagination.
</ResponseField>

<ResponseField name="has_more" type="boolean">
  Indique si une autre page existe après celle-ci.
</ResponseField>

<ResponseExample>

```json Response
{
  "success": true,
  "training_jobs": [],
  "count": 0,
  "total": 0,
  "has_more": false
}
```

</ResponseExample>

Des valeurs de pagination invalides renvoient `422`.
