---
title: "Fastino API 身份验证:生成和使用 API 密钥"
sidebarTitle: "身份验证"
description: "使用 X-API-Key 头对 Fastino API 请求进行身份验证。在控制台创建 API 密钥,并通过 REST API 列出、轮换或撤销密钥。"
---

大多数 Fastino API 操作都需要您的 API 密钥。明确公开的目录路由(例如 `GET /v1/models`)无需密钥即可调用。需要身份验证的操作采用简单的基于请求头的方案,无需 OAuth 流程或令牌交换。您的密钥用于标识您的身份,并决定哪些资源和速率限制适用于您的请求。

## 获取 API 密钥

1. 在 [Fastino](https://agent.fastino.ai) 登录。
2. 前往 **Settings** → **API Keys**。
3. 点击 **Create key**,为其命名,并复制显示的值。

请将您的密钥存储在环境变量中(例如 `FASTINO_API_KEY`),而不是硬编码在源文件中。

新的 Fastino 密钥以 `fast_sk_` 开头。旧版 `pio_sk_` 密钥已停用;在调用 Fastino API 之前,请先在控制面板中创建替换密钥。

## 传递 API 密钥

在原生 Fastino 请求的 `X-API-Key` 头中包含您的密钥:

```bash cURL
curl https://api.fastino.ai/v1/models \
  -H "X-API-Key: $FASTINO_API_KEY"
```

OpenAI 和 Anthropic 兼容的 SDK 通常会将同一密钥作为 Bearer 令牌发送。将其 `api_key` 选项设置为 `$FASTINO_API_KEY`;SDK 会自动为您添加 `Authorization: Bearer ...`。

## 管理密钥

请从 Fastino 控制面板的 **Settings** -> **API Keys** 创建新的 API 密钥。使用 API 密钥进行身份验证的请求可以列出和撤销密钥,但无法创建更多密钥。

### 创建密钥

`POST /v1/create-api-key` 供 Web 控制面板使用,需要浏览器会话。使用 `X-API-Key` 进行身份验证的调用会返回 `403 Forbidden`,并附带消息 `API key creation is only allowed from the web dashboard.`

创建响应包含新的 `secret_key` 值。请立即复制它,该值不会再次显示。

### 列出密钥

```bash cURL
curl https://api.fastino.ai/v1/list-api-keys \
  -H "X-API-Key: YOUR_API_KEY"
```

返回与您账户关联的所有密钥,包括名称和创建日期。列表响应中不返回密钥值。

### 撤销密钥

```bash cURL
curl -X DELETE https://api.fastino.ai/v1/delete-api-key \
  -H "X-API-Key: YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "key_id": "YOUR_KEY_ID"               
  }'
```

被撤销的密钥在下一个请求时会立即被拒绝。无法撤销此操作。

### 在获得密钥前测试连通性

要在集成期间验证您的网络能否访问 Fastino API,请使用占位符密钥发送请求。您会收到 `401` 响应,这表明该端点可达,并且您的请求已正确连接。

```bash
curl -X POST https://api.fastino.ai/v1/messages \
  -H "X-API-Key: fast_sk_test" \
  -H "Content-Type: application/json" \
  -d '{"model":"claude-haiku-5","max_tokens":10,"messages":[{"role":"user","content":"hi"}]}'

# Expected: {"detail":"Invalid API key format. API keys must start with 'fast_sk_'. Please check your X-API-Key header."}
# A 401 with this body = integration is wired correctly. Swap in a real key to get completions.
```

## 错误响应

| 状态码 | 含义 |
| --- | --- |
| `401 Unauthorized` | 密钥缺失、格式错误或已被撤销。请检查是否存在 `X-API-Key` 头,并确保其中包含有效的密钥。 |
| `402 Payment Required` | 您的账户积分不足以完成请求。请前往 **Settings** → **Billing** 为余额充值。 |

<Warning>
  `402` 响应表示您的账户积分已用尽。请求将持续失败,直至您添加积分或升级套餐。请参阅 [Plans & Pricing](/pricing) 了解可选方案。
</Warning>
