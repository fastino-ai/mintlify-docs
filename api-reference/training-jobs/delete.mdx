---
title: "Delete a training job"
sidebarTitle: "Delete job"
description: "Delete a Fastino training job and its associated checkpoints."
api: "DELETE /v1/felix/training-jobs/{job_id}"
noindex: true
---

`DELETE /v1/felix/training-jobs/{job_id}`

Deletes the job and its stored artifacts. If the job is active, the service also stops it.

<Warning>
  Deletion is irreversible. First download anything you need into storage you control. Deletion removes checkpoints and stored artifacts and releases active hosted deployments.
</Warning>

## Request

<ParamField header="X-API-Key" type="string" required>
  Your Fastino API key.
</ParamField>

<ParamField path="job_id" type="string" required>
  Training-job UUID.
</ParamField>

<RequestExample>

```bash cURL
curl -X DELETE https://api.fastino.ai/v1/felix/training-jobs/YOUR_TRAINING_JOB_ID \
  -H "X-API-Key: $FASTINO_API_KEY"
```

</RequestExample>

## Response

<ResponseExample>

```json Response
{
  "success": true,
  "message": "Training job deleted"
}
```

</ResponseExample>

An unknown or inaccessible job returns `404`.
