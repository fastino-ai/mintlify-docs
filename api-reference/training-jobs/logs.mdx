---
title: "Get training logs"
sidebarTitle: "Get logs"
description: "Retrieve a point-in-time snapshot of a Fastino training job's output logs."
api: "GET /felix/training-jobs/{job_id}/logs"
noindex: true
---

`GET /felix/training-jobs/{job_id}/logs`

Returns structured stdout and stderr entries. This endpoint does not stream; poll it to follow a running job.

## Request

<ParamField header="X-API-Key" type="string" required>
  Your Fastino API key.
</ParamField>

<ParamField path="job_id" type="string" required>
  Training-job UUID.
</ParamField>

```bash
curl https://api.pioneer.ai/felix/training-jobs/YOUR_TRAINING_JOB_ID/logs \
  -H "X-API-Key: YOUR_API_KEY"
```

## Response

<ResponseField name="job_id" type="string">
  Training-job UUID.
</ResponseField>

<ResponseField name="logs" type="object[]">
  Log entries. Each entry contains `id`, `job_id`, `timestamp`, `level`, `message`, and `source`.
</ResponseField>

<ResponseField name="total_logs" type="integer">
  Number of entries returned.
</ResponseField>

```json
{
  "job_id": "YOUR_TRAINING_JOB_ID",
  "logs": [
    {
      "id": "LOG_ID",
      "job_id": "YOUR_TRAINING_JOB_ID",
      "timestamp": "2026-09-22T20:00:00Z",
      "level": "INFO",
      "message": "Epoch 2 completed",
      "source": "trainer"
    }
  ],
  "total_logs": 1
}
```

An unknown or inaccessible job returns `404`.
