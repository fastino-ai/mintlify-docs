---
title: "Deploy a training checkpoint"
sidebarTitle: "Deploy checkpoint"
description: "Deploy a saved Fastino training checkpoint to hosted inference."
noindex: true
---

`POST /felix/training-jobs/{job_id}/checkpoints/{checkpoint_id}/deploy`

Deploys one deployable checkpoint to the hosted multi-model inference endpoint.

## Request

<ParamField header="X-API-Key" type="string" required>
  Your Fastino API key.
</ParamField>

<ParamField path="job_id" type="string" required>
  Training-job UUID.
</ParamField>

<ParamField path="checkpoint_id" type="string" required>
  Checkpoint ID returned by the checkpoint-list endpoint.
</ParamField>

```bash
curl -X POST \
  https://api.pioneer.ai/felix/training-jobs/YOUR_TRAINING_JOB_ID/checkpoints/CHECKPOINT_ID/deploy \
  -H "X-API-Key: YOUR_API_KEY"
```

## Response

```json
{
  "success": true,
  "message": "Checkpoint deployed",
  "job_id": "YOUR_TRAINING_JOB_ID",
  "checkpoint_id": "CHECKPOINT_ID",
  "mme_path": "PROVIDER_MODEL_PATH"
}
```

An unknown job or checkpoint returns `404`. A checkpoint that is not deployable returns a request-specific `4xx` error.
