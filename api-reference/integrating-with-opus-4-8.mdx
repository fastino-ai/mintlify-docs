---
title: "Set up Opus 4.8 in 60s"
---

## Integrating Pioneer with Opus 4.8

Point your agent at Pioneer's inference endpoint and switch between models using each agent's native `/model` command.

1. Download and [set up](https://code.claude.com/docs/en/quickstart) Claude Code.
2. Follow the [Claude Code integration](/claude-code) guide to point Claude Code at Pioneer, enable gateway model discovery, and launch with `pioneer/auto`.Follow the [Claude Code integration](/claude-code) guide to point Claude Code at Pioneer, enable gateway model discovery, and launch with `pioneer/auto`.
3. Use `/model` to switch models. Pioneer exposes Opus 4.8 and other decoder models via gateway discovery aliases on `GET /v1/modelUse /model` to switch models. Pioneer exposes Opus 4.8 and other decoder models via gateway discovery aliases on `GET /v1/models`.