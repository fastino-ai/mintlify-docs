---
title: "Renombrar un modelo entrenado"
sidebarTitle: "Renombrar modelo"
description: "Actualiza el nombre para mostrar asociado a un trabajo de entrenamiento de Fastino."
api: "PATCH /felix/training-jobs/{job_id}/model-name"
noindex: true
---

Actualiza el nombre del modelo en un trabajo de entrenamiento.

## Solicitud

<ParamField header="X-API-Key" type="string" required>
  Tu clave de API de Fastino.
</ParamField>

<ParamField path="job_id" type="string" required>
  UUID del trabajo de entrenamiento.
</ParamField>

<ParamField body="model_name" type="string" required>
  Nuevo nombre del modelo. Longitud: 1–64 caracteres. Usa letras, números, guiones o guiones bajos.
</ParamField>

<RequestExample>

```bash cURL
curl -X PATCH https://api.pioneer.ai/felix/training-jobs/YOUR_TRAINING_JOB_ID/model-name \
  -H "X-API-Key: $PIONEER_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model_name": "my-model-name"}'
```

</RequestExample>

## Respuesta

<ResponseExample>

```json Response
{
  "success": true,
  "message": "Model name updated",
  "job_id": "YOUR_TRAINING_JOB_ID",
  "model_name": "my-model-name"
}
```

</ResponseExample>

Los nombres inválidos devuelven `422`. Un trabajo desconocido o inaccesible devuelve `404`.
